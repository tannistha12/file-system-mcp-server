# Development Rules

- Keep functions small.
- Use pathlib instead of string paths where possible.
- Validate every path.
- Never access files outside the workspace.
- Add docstrings and type hints.
- Handle errors gracefully.
- Write clear commit messages.
